from datetime import datetime


def log(text, module=None):
    now = datetime.now()
    c_time = now.strftime("%H:%M:%S")
    if module is None:
        a = f'[{c_time}]: {text}'
    else:
        a = f'[{c_time}] [{module}]: {text}'

    print(a)
